<?xml version="1.0" encoding="UTF-8"?>
<APP	xmlns="http://www.jessma.org"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://www.jessma.org http://www.jessma.org/schema/app-config-3.5.xsd">

    <!-- 系统配置（必填） -->
    <system>
        <app-life-cycle-listener class="com.lesdo.standalone.global.SyxyAppLifeCycleListener" />
        <user-config-parser class="com.lesdo.standalone.global.SyxyConfigParser" />
    </system>

    <!-- 用户配置（可选） -->
    <user>
        <syxy-home>syxy</syxy-home>
    </user>
</APP>
